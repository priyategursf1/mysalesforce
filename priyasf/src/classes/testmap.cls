public class testmap {

    public static void display()
    {
        map<integer,string> EA = new map<integer,string>();
        ea.put(1, 'ind');
        ea.put(2, 'usa');
        system.debug('address of 1 is:'+ea.get(1));
        system.debug('the size od address:'+ea.size());
    }
}