public class testset {
  
    public static void display() {
        set<integer> si =new set<integer> {20,10,30,40};
       // si.sort();
      //  system.debug(si[0]);
        system.debug('the length of set is:'+ si.size());
        
        system.debug('display whole set'+si);
        system.debug('the set cintains 40:'+si.contains(40));
    }
}